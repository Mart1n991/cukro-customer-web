import { Box, Typography } from "@mui/material";
import Layout from "../layout/Layout";

const Error = () => (
  <Layout>
    <Box>
      <Typography variant="body1">Dáta sa nepondarilo načítať :(</Typography>
    </Box>
  </Layout>
);

export default Error;
