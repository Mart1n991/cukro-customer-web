import { Typography } from "@mui/material";
import Layout from "../../components/layout/Layout";

const Cakes = () => (
  <Layout>
    <Typography>TORTY</Typography>
  </Layout>
);

export default Cakes;
